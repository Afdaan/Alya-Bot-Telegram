version: "3.9"
services:
  alya-bot:
    build: .
    container_name: alya-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - alya-network

networks:
  alya-network:
    driver: bridge